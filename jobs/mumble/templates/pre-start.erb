LOG_DIR=/var/vcap/sys/log/mumble

# looks like it always get an exit 1 when changing pasword
/var/vcap/packages/mumble/murmur.x86 -ini /var/vcap/jobs/mumble/config/murmur.ini -supw '<%= p("mumble.superuser.password") %>' >> $LOG_DIR/pre-start.stdout.log 2>> $LOG_DIR/pre-start.stderr.log || true
